# .github/workflows/release.yaml

on:
  push

jobs:
  build:
    name: Build Go
    runs-on: ubuntu-latest
    steps:
    - name: linux build
    - uses: actions/checkout@v3
    - uses: actions/setup-go@v3
    - run: go version
    - run: go mod download
    - run: go build -o ht